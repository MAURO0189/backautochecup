import { Injectable } from '@nestjs/common';

@Injectable()
export class AuthService {
  // Aquí puedes agregar métodos relacionados con la autenticación, como validar usuarios, generar tokens, etc.
}
